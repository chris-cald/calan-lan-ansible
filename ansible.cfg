[defaults]
# Use the stdout_callback when running ad-hoc commands.
bin_ansible_callbacks = True
# Let playbooks below playbooks/ resolve repository-owned roles.
roles_path = ./roles

# Set the output format for Ansible playbooks and ad-hoc commands.
# callback_result_format = yaml

# interpreter_python=/usr/bin/python3
- name: Make sure QEMU Guest Agent is installed
  become: true
  package:
    name:
      - qemu-guest-agent
    state: latest
  # when: is a proxmox vm
- name: Make sure QEMU Guest Agent is running
  systemd_service:
    state: started
    name: qemu-guest-agent

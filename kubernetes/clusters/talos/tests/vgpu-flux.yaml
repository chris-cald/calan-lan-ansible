apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: vgpu-test
  namespace: flux-system
spec:
  interval: 10m
  path: ./kubernetes/clusters/talos/tests/vgpu
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  suspend: true

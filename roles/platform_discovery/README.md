# Platform discovery

Collects read-only architecture, PVE, PCI/NVIDIA, IOMMU, and `mdevctl` facts,
even under Ansible check mode. It classifies eligible role entrypoints and
lists non-discoverable prerequisites without selecting or applying them.

#SPDX-License-Identifier: MIT-0
---
# tasks file for sudo
- name: Load system-specific variables
  ansible.builtin.import_tasks: load_system_vars.yml

- name: Ensure user '{{ privileged_user }}' does not have privileged access. Optionally delete user
  ansible.builtin.import_tasks: remove_privilege.yml
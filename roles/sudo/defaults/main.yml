#SPDX-License-Identifier: MIT-0
---
# defaults file for sudo
password_required: true
# Empty preserves the legacy NOPASSWD:ALL behavior when password_required is false.
passwordless_commands: []

# Compatibility aliases: when set, sudo_* values override legacy inputs.
# sudo_privileged_user
# sudo_privileged_group
# sudo_password_required
# sudo_preserve_user

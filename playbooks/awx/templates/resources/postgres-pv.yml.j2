---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres
spec:
  storageClassName: "{{ postgres_storage_class }}"
  capacity:
    storage: 11Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "{{ postgres_storage_path }}"
